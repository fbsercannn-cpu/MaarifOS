declare const __MAARIF_BUILD__: {
  readonly version: string;
  readonly revision: string;
  readonly sourceSha256: string;
  readonly builtAt: string;
};

export const BUILD_IDENTITY = __MAARIF_BUILD__;
